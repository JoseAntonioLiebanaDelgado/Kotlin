<?xml version="1.0" encoding="utf-8"?>
<!-- Contenedor principal de la interfaz de usuario, usando ConstraintLayout para un diseño flexible y responsivo. -->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- Primer campo de texto editable para la entrada del usuario, centrado horizontalmente y con un margen superior. -->
    <EditText
        android:id="@+id/inputText1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:autofillHints=""
        android:ems="10"
        android:hint="@string/value"
        android:inputType="text"
        android:minHeight="48dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="DuplicateSpeakableTextCheck" />

    <!-- Segundo campo de texto editable, similar al primero pero posicionado debajo de él. -->
    <EditText
        android:id="@+id/inputText2"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:autofillHints=""
        android:ems="10"
        android:hint="@string/value"
        android:inputType="text"
        android:minHeight="48dp"
        app:layout_constraintEnd_toEndOf="@+id/inputText1"
        app:layout_constraintStart_toStartOf="@+id/inputText1"
        app:layout_constraintTop_toBottomOf="@+id/inputText1" />

    <!-- Botón para iniciar la operación de suma, ubicado debajo del segundo campo de texto. -->
    <Button
        android:id="@+id/buttonSuma"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="@string/suma"
        app:layout_constraintEnd_toEndOf="@+id/inputText1"
        app:layout_constraintStart_toStartOf="@+id/inputText1"
        app:layout_constraintTop_toBottomOf="@+id/inputText2" />
</androidx.constraintlayout.widget.ConstraintLayout>
